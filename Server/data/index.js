const usersData = require('./users');
const eventsData = require('./events');
const reportsData = require('./reports');

module.exports = {
	users: usersData,
	events: eventsData,
	reports: reportsData
};
